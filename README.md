# book-review-API-serverless-AWS

Serverless API backend on AWS for Book Review Application

## Services

The whole API is divided into different services (representing database tables).

1. book: CRUD APIs and lambda functions, interacting with `book-table` in DynamoDB

2. category: CRUD APIs and lambda functions, interacting with `category-table` in DynamoDB
